# Dragonfortune - Server .env template
#
# Cara pakai:
# 1) Di server, buka file `.env` (mis. `nano .env`)
# 2) Copy-paste isi file ini
# 3) Ganti value yang bertanda CHANGE_ME
# 4) Jalankan `bash scripts/server-setup.sh`
#
# Catatan:
# - Jangan commit `.env` ke GitHub (sudah di-ignore).
# - APP_KEY biarkan kosong; nanti di-generate otomatis oleh `server-setup.sh`.
#

APP_NAME=Dragonfortune
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://dragonfortune.ai

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=info

# --- Database (Paling gampang: sqlite) ---
DB_CONNECTION=sqlite
DB_DATABASE=/www/wwwroot/dragonfortune.ai/database/database.sqlite

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

BACKTEST_RESULTS_DISK=local
BACKTEST_RESULTS_DIR=backtest-results

CACHE_STORE=database

# --- Mail (aman buat production, tidak kirim email) ---
MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

VITE_APP_NAME="${APP_NAME}"

# --- Dragonfortune / Integrations ---
# Coinglass (WAJIB diisi biar halaman data tidak error)
COINGLASS_API_URL=https://open-api-v4.coinglass.com/api
COINGLASS_API_KEY=CHANGE_ME
COINGLASS_TIMEOUT=15
COINGLASS_RETRIES=2

# Optional caching TTLs
# - COINGLASS_CACHE_TTL is minutes (macro overlay); others are seconds.
COINGLASS_CACHE_TTL=15
COINGLASS_ETF_CACHE_TTL=30
COINGLASS_VOLATILITY_CACHE_TTL=30
COINGLASS_FR_CACHE_TTL=10
COINGLASS_OI_CACHE_TTL=10
COINGLASS_LSR_CACHE_TTL=10
COINGLASS_LIQUIDATIONS_CACHE_TTL=10
COINGLASS_BASIS_CACHE_TTL=10
COINGLASS_SENTIMENT_CACHE_TTL=10

# CryptoQuant (opsional)
CRYPTOQUANT_API_KEY=

# FRED (opsional, macro overlay)
FRED_API_KEY=

# External API base URL (opsional)
API_BASE_URL=
SPOT_MICROSTRUCTURE_API_URL=
SPOT_USE_COINGLASS=true
SPOT_STUB_DATA=false

